# Used on CI

import os ;

local OPENSSL_ROOT = [ os.environ OPENSSL_ROOTT ] ;

using openssl
    :
    :
        <target-os>windows<variant>release:<search>"$(OPENSSL_ROOT)/lib"
        <target-os>windows<variant>debug:<search>"$(OPENSSL_ROOT)/debug/lib"
        <target-os>windows:<include>$(OPENSSL_ROOT)/include
    ;
